import { createSlice } from "@reduxjs/toolkit";

const initialState = {};
const liveSlice = createSlice({
  name: "liveSlice",
  initialState,
  reducers: {},
  extraReducers: {},
});
export default liveSlice.reducer;
